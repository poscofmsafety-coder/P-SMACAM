# V4.2 보호구 감지 강제 보완

## 확인된 원인

- 기존 장치의 저장 설정에서 `inferMissingPpeFromPerson`이 비활성화된 경우 새 코드의 보완 판정이 실행되지 않을 수 있었습니다.
- 낮은 신뢰도의 `Goggles` 또는 `Mask` 오검출이 존재하면 실제 착용으로 잘못 간주해 `NO-Goggles`, `NO-Mask` 보완 상자를 만들지 않는 구조였습니다.

## 수정사항

- 설정 버전을 6으로 올리고 기존 장치 설정을 V4.2 보호구 보완 설정으로 자동 마이그레이션
- `NO-Hardhat`만 검출된 근거리 얼굴 장면에서도 보안경·마스크 영역을 강제로 확인
- 보안경·마스크 착용 판정에 최소 신뢰도, 예상 영역 포함 여부, 크기 비율을 모두 적용
- 약한 착용 오검출은 제거하고 `GOG: NF CHECK`, `MASK: NF CHECK` 생성
- 3개 보호구 경고 배너와 음성을 순차 재생
- 정적 파일 캐시 버전 `4.2.0` 적용

## 표시 의미

- `HAT: NF`: 안전모 미착용 클래스 직접 검출 또는 보완 판정
- `GOG: NF CHECK`: 보안경 착용이 충분한 신뢰도로 확인되지 않아 현장 확인 필요
- `MASK: NF CHECK`: 마스크 착용이 충분한 신뢰도로 확인되지 않아 현장 확인 필요
