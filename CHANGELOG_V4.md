# V4 변경사항

## 작업중지권

- 현장 사용자 화면에 빨간색 `작업중지권` 버튼 추가
- 요청자 이름·연락처·작업중지 사유 입력
- 요청 순간 현장 스냅숏 저장
- 관리자 화면 긴급 모달·반복 알람·브라우저 알림
- 관리자 `접수 → 작업중지 확인 → 조치 중 → 해제/종료` 상태관리
- 요청 직후 관제센터 무전 자동호출 선택
- Cloudflare Email Service 메일 발송 및 외부 메시지 Webhook 연동

## 현장 사진 AI 분석

- 사용자와 관리자 모두 현재 카메라 장면 캡처 가능
- 첨부된 고정 산업안전 분석 프롬프트를 서버 모듈에 포함
- 객관적으로 사진에서 확인되는 위험만 분석
- S×L 위험점수·확실도·즉시조치 형식 적용
- 분석결과 화면 표시·한국어 음성 읽기·담당자 이메일 발송
- 이벤트 센터에서 기존 AI 분석결과 재열람

## TBM 안전소통

- D-안전회의 메일 본문을 AI가 TBM 요점으로 변환
- 현장 사용자 화면에서 요점 음성 재생
- 작업자 의견 수집·AI 보완 위험요인 생성
- 담당자 이메일 발송
- 작업자 의견과 AI 보완결과를 다음 TBM 생성 시 참고하는 누적 구조
- Gmail Apps Script 자동수신 예제 포함

## 실시간 관제

- WebRTC P2P 연속 영상 유지
- 대화면 관제는 고화질 30fps 목표, 카드 화면은 저비트레이트 모드
- H.264/VP8 우선, 프레임 유지형 품질저하 정책
- ICE 실패 시 자동 재연결 및 TURN 설정 확장
- 여러 카드 전체를 재생성하지 않고 기존 video 요소를 유지해 UI 정지 감소
