# V4.1 PPE 다중 미착용 경고 보완

## 수정 원인

기존 버전은 `Person` 클래스가 검출된 경우에만 안전모·보안경·마스크의 미착용 보조 판정을 생성했습니다. 가까운 웹캠 화면에서는 모델이 사람 전체를 `NO-Hardhat`으로만 검출하고 `Person`을 놓치는 경우가 있어 안전모 경고만 표시될 수 있었습니다.

## 변경사항

- `Person`이 없더라도 `NO-Hardhat`, `Hardhat`, `Goggles`, `NO-Goggles`, `Mask`, `NO-Mask` 검출을 작업자 앵커로 활용
- 안전모 경고 상자만 검출된 장면에서도 얼굴의 눈 영역과 입·코 영역을 추정하여 다음 보조 경고 생성
- 웹캠 근접 촬영은 상반신·얼굴 비율을 따로 판정하여 보안경 상자는 눈 주변, 마스크 상자는 코·입 주변에 표시
  - `GOG: NF ... CHECK`
  - `MASK: NF ... CHECK`
- 보안경·마스크 음성 경고와 이벤트 업로드가 3회 연속 확인 후 각각 실행
- `Person` 클래스 문턱값을 낮춰 근거리 웹캠 장면의 작업자 검출 보완
- 브라우저가 이전 `ppe-worker.js`를 계속 쓰지 않도록 캐시 정책 변경

## 표시 의미

- `NF`: 미착용 의심
- `CHECK`: 전용 미착용 클래스가 직접 잡힌 것이 아니라 작업자/얼굴 앵커에서 착용 보호구가 확인되지 않아 관리자 확인이 필요한 보조 판정

## 적용 후 확인

GitHub 덮어쓰기 후 Cloudflare 배포가 끝나면 관리자·사용자 브라우저 모두 `Ctrl + F5`로 강력 새로고침합니다. 모바일은 브라우저 탭을 닫고 다시 접속합니다.
