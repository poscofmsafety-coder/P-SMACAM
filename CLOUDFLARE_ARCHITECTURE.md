# Cloudflare 배포 아키텍처 V4

```text
┌──────────────────────── 현장 노트북/휴대폰 ────────────────────────┐
│ getUserMedia 카메라                                                │
│ PPE ONNX Web Worker                                                │
│ 한국어 음성경고                                                    │
│ 작업중지권·사진캡처·TBM 의견                                      │
│ WebRTC 영상·무전 ───────────────────────────────────────────┐      │
└───────────────────────────────┬──────────────────────────────┼──────┘
                                │ REST/WebSocket                │ P2P media
                                ▼                               │
┌──────────────────────── Cloudflare Worker ───────────────────┐│
│ Static Assets 관리자·사용자 UI                               ││
│ HMAC Session 인증                                            ││
│ REST API                                                     ││
│ Workers AI / OpenAI 선택형 AI                                ││
│ Email Sending API / Message Webhook                          ││
│ Durable Object SignalingRoom ────────────────────────────────┘│
└───────────────┬────────────────┬───────────────────────────────┘
                │                │
                ▼                ▼
       ┌──────────────┐  ┌──────────────────┐
       │ Cloudflare D1│  │ 외부 연동         │
       │ 장치·이벤트   │  │ Gmail Apps Script │
       │ 작업중지권     │  │ Email Service     │
       │ 사진·AI 결과   │  │ Message Webhook    │
       │ TBM·작업자의견 │  └──────────────────┘
       └──────────────┘
```

## 1. 브라우저 Edge Agent

별도 설치 프로그램 없이 동일 사이트의 사용자 모드가 Edge Agent 역할을 합니다.

- 카메라 입력
- PPE 모델 추론
- 위험구역 판정
- 이벤트 스냅숏
- 작업중지권
- 사진 AI 분석 요청
- TBM 조회·의견 제출
- 현장 한국어 음성

## 2. 실시간 영상과 무전

Durable Object는 SDP/ICE/호출 이벤트 같은 신호만 전달합니다. 영상과 음성은 WebRTC로 현장과 관리자 브라우저 사이에 직접 전달됩니다.

- 카드 영상: 저비트레이트
- 클릭한 대화면: 고화질 30fps 목표
- 통화 오디오: 영상과 분리된 마이크 전용 PeerConnection
- P2P 실패 환경: TURN 설정
- 대규모 운영: Cloudflare Realtime SFU 확장

## 3. 작업중지권

```text
작업자 빨간 버튼
→ 사유·요청자·현재 사진 제출
→ D1 요청·긴급 이벤트 기록
→ Durable Object로 관리자 실시간 방송
→ 관리자 반복 알람·브라우저 알림
→ 이메일·메시지 Webhook
→ 선택 시 무전 자동호출
→ 관리자 상태관리
```

## 4. 사진 AI 분석

```text
카메라 프레임 캡처
→ D1 저용량 사진 저장
→ 고정 안전분석 프롬프트
→ Workers AI, 실패 시 OpenAI
→ 분석결과·위험등급 저장
→ 사용자/관리자 화면·음성·메일
```

분석 프롬프트는 사진에서 객관적으로 확인할 수 있는 위험만 작성하고, 사진으로 알 수 없는 법령 준수·교육·작업허가·위험성평가 여부를 단정하지 않도록 구성되어 있습니다.

## 5. TBM 안전소통

```text
D-안전회의 메일
→ 관리자 붙여넣기 또는 Gmail 자동연동
→ AI 작업내용·위험요인·안전조치 요약
→ 활성 TBM으로 사용자 화면 전파
→ 음성 재생
→ 작업자 의견 입력
→ AI 추가 위험·보완조치 생성
→ 담당자 이메일
→ D1에 누적되어 다음 TBM 참고
```

## 6. 저장정책

D1 무료 시연판을 고려해 사진은 1.5MB 이하로 제한합니다.

- 실시간 영상: 저장하지 않음
- 프리뷰: 장치별 최신 1장 덮어쓰기
- 이벤트·작업중지·AI 분석: 필요한 순간 사진만 저장
- 운영형 환경: R2 또는 사내 영상저장소로 분리 권장

## 7. 보안

- 관리자/사용자 역할 분리
- HMAC 서명 HttpOnly 세션 쿠키
- 관리자 API 권한 검사
- TBM 수신 전용 Secret Token
- 카메라·마이크는 HTTPS와 사용자 명시적 동의 필요
- Secret은 GitHub가 아닌 Cloudflare Secret 사용
- 운영 전 개인정보·영상정보 접근권한과 보존기간 정책 적용
